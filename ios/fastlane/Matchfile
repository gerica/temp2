# git_url("https://github.com/radiolife-labs/ios-credentials.git")
git_url("ssh://git@github.com:radiolife-labs/ios-credentials.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

# app_identifier(["tools.fastlane.app", "tools.fastlane.app2"])
# username("user@fastlane.tools") # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
#
# git_url "ssh://git@bitbucket.org:7999/abc/project-match.git" #your private git repo ssh link
# type "development" # Either development, appstore etc.
# app_identifier 'com.project1.appname'
# username "member@companyname.com" #This will be the git username
# shallow_clone true
# force_for_new_devices true
# ENV["FASTLANE_PASSWORD"] = "abcdefgh" #Password to access git repo.
# ENV["MATCH_PASSWORD"] = "password" #Password for the .p12 files saved in git repo.
