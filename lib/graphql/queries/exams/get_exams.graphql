query GetExams{
    examResultMany {
        date
        examNumber
        result
        deviceId
        serialNumber
        phase
        device {
            locate
            type
        }
    }
}