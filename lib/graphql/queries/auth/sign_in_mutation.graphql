mutation SignIn($email: String!, $password: String!){
    userLogin(record: {email: $email, password: $password}) {
        _id
        token
        firstName
        lastName
        email
        confirmed
    }
}