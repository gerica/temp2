mutation ChangePassword($currentPassword: String!, $newPassword: String!){
    userChangePassword(record: {newPassword: $newPassword, oldPassword: $currentPassword}){
        _id
        token
        firstName
        lastName
        email
        confirmed
    }
}