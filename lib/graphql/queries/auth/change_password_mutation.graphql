mutation ChangePassword($currentPassword: String!, $newPassword: String!){
    userChangePassword(input: {newPassword: $newPassword, oldPassword: $currentPassword}){
        _id
        token
        firstName
        lastName
        email
        confirmed
    }
}