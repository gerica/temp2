mutation SignUp(
    $firstName: String!,
    $lastName: String!,
    $email: String!,
) {
    userRegister(
        record: {
            firstName: $firstName,
            lastName: $lastName,
            email: $email,
        }
    )
}