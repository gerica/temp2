mutation SignUp(
    $firstName: String!,
    $lastName: String!,
    $email: String!,
) {
    userRegister(
        input: {
            firstName: $firstName,
            lastName: $lastName,
            email: $email,
        }
    )
}